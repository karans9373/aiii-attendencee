version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///./workpulse.db
      SECRET_KEY: change-me-in-production

  web:
    build: ./web
    ports:
      - "8080:80"
    depends_on:
      - backend
